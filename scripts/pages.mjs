import {escape as e, textLink, tags, label, heading, profiles, portrait, framework, trajectory, paperReference, publication, featured, pageHero, environment, authors} from './components.mjs';

export function home(d) {
  const published = d.publications.filter(p => !p.status);
  const firstAuthor = published.filter(p => p.type === 'first');
  const registered = d.patents.filter(p => p.status === 'Registered');
  return `<section class="home-hero shell">
    <div class="home-hero-grid"><div class="home-hero-copy"><p class="home-name">MIN JONG LEE</p><h1>Interfaces, defects, and devices <br>for future memory and computing.</h1><p class="home-deck">I study how molecular interfaces, defects, and ionic dynamics govern emerging electronic devices—from memristive memory to integrated computing hardware.</p><p class="home-affiliation">Korea University · Electrical Engineering</p><div class="home-actions"><a class="text-link" href="research.html">Research <span aria-hidden="true">→</span></a><a class="text-link" href="publications.html">Publications <span aria-hidden="true">→</span></a><a class="text-link" href="${e(d.profile.cv)}" download>CV <span aria-hidden="true">↓</span></a></div></div>${portrait(d)}</div>
  </section>
  <section class="home-highlights shell" aria-labelledby="highlights-title"><div class="home-section-head"><h2 id="highlights-title">Selected highlights</h2></div><div class="highlight-grid"><a href="publications.html"><strong>${published.length}</strong><span>Published papers</span></a><a href="publications.html"><strong>${firstAuthor.length}</strong><span>First-author papers</span></a><a href="patents.html"><strong>${registered.length}</strong><span>Registered patents</span></a></div><p class="highlight-source">Based on the CV updated ${e(d.profile.updated)}.</p></section>
  <section class="home-research shell" aria-labelledby="home-research-title"><div class="home-section-head"><h2 id="home-research-title">Research</h2></div><div class="home-research-list"><a href="research.html#interfaces"><span>01</span><h3>Interface &amp; Defect Physics</h3><p>Molecular interfaces, electronic structure, traps, and ionic dynamics.</p></a><a href="research.html#memory"><span>02</span><h3>Emerging Memory Devices</h3><p>Resistive and capacitive devices, switching physics, variability, and reliability.</p></a><a href="research.html#integration"><span>03</span><h3>Integrated Electronics &amp; Computing</h3><p>Long-term direction: thin-film oxide electronics, device integration, and hardware-aware computing.</p></a></div><p class="home-foundation">Connected foundation: <a href="research.html#optoelectronics">Organic &amp; Hybrid Optoelectronics</a>.</p></section>
  <section class="home-featured" aria-labelledby="featured-title"><div class="shell"><div class="home-section-head"><h2 id="featured-title">Featured work</h2>${textLink('publications.html','All publications')}</div>${featured(d)}</div></section>
  <section class="home-about shell" aria-labelledby="home-about-title"><h2 id="home-about-title">About</h2><div><p>Min Jong Lee is an integrated M.S.–Ph.D. researcher in electrical engineering at Korea University. He works with Prof. Jae Won Shim in the Advanced Electronic Device &amp; Energy Conversion System Laboratory.</p>${textLink('about.html','More about Min Jong Lee')}</div></section>`;
}

export function research(d) {
  return `${pageHero('Research / Framework','Interface &amp; defect engineering<br>for <em>memory and computing.</em>','A research trajectory built around molecular control, electronic and ionic transport, and device function. Integration and computing provide the long-term direction.')}<div class="shell framework-page">${framework()}</div><div class="section shell"><nav class="section-nav" aria-label="Research pillars">${d.pillars.map(p=>`<a href="#${p.id}">${p.number} / ${e(p.title)}</a>`).join('')}<a href="#optoelectronics">Connected research</a></nav>${d.pillars.map(p=>`<section class="research-detail" id="${p.id}"><div class="research-detail-heading"><span class="section-number">${p.number}</span>${label(p.subtitle)}<h2>${e(p.title)}</h2><p class="status-line">${e(p.status)}</p></div><div class="research-detail-body"><h3 class="scientific-question">${e(p.question)}</h3><p>${e(p.motivation)}</p><div class="detail-columns"><div><h3 class="small-heading">Current research</h3><p>${e(p.current)}</p></div><div><h3 class="small-heading">Established work / selected results</h3><p>${e(p.results)}</p></div></div><h3 class="small-heading">Methods &amp; capabilities</h3>${tags(p.methods)}<div class="future-note"><h3>Emerging / future directions</h3><p>${e(p.future)}</p></div><h3 class="small-heading">Relevant publications</h3><ul class="paper-references">${p.papers.map(id=>paperReference(d,id)).join('')}</ul></div></section>`).join('')}<section class="research-detail" id="optoelectronics"><div class="research-detail-heading">${label('Connected research')}<h2>${e(d.connected.title)}</h2></div><div class="research-detail-body"><p class="lead">${e(d.connected.text)}</p><p>This background developed within <a href="${d.profile.labUrl}">AEEL at Korea University</a>. Charge-selective interfaces and transparent electrodes link optoelectronic function to the same transport questions that now motivate my memory research.</p>${tags(['Indoor photovoltaics','Photodetectors','Transparent electrodes','Charge-selective interfaces'])}<ul class="paper-references">${d.connected.papers.map(id=>paperReference(d,id)).join('')}</ul></div></section></div>${projects(d)}`;
}

export function publications(d) {
  const published=d.publications.filter(p=>!p.status).sort((a,b)=>(a.type==='first'?0:1)-(b.type==='first'?0:1)||b.year-a.year);
  const manuscripts=d.publications.filter(p=>p.status);
  return `${pageHero('Publications','The research <em>record.</em>','Work across molecular interfaces, emerging memory, thin-film electrodes, and organic optoelectronics.',`<div class="link-row">${textLink(d.scholar,'Google Scholar')}<button type="button" class="text-link print-button" hidden>Print publication list <span aria-hidden="true">↗</span></button></div>`)}<section class="section shell publications-section" id="publications"><h2 class="sr-only">Published papers</h2><div class="publication-controls" role="group" aria-label="Filter publications by research topic" hidden><button class="filter-button active" type="button" data-filter="all" aria-pressed="true">All</button>${d.topics.map(t=>`<button class="filter-button" type="button" data-filter="${t.id}" aria-pressed="false">${e(t.label)}</button>`).join('')}</div><div class="publication-intro"><p id="publication-count" role="status" aria-live="polite">${published.length} published papers</p><span>First-author work, followed by collaborative publications</span></div><div id="publication-list">${published.map(p=>publication(d,p)).join('')}</div><p class="record-note">${e(d.notes.authors)}</p><section class="manuscripts" aria-labelledby="manuscripts-title"><div class="section-heading"><div>${label('Work in progress')}<h2 id="manuscripts-title">Manuscripts</h2></div></div>${manuscripts.map(p=>publication(d,p)).join('')}<p class="record-note">Status as recorded in the CV updated September 2, 2026. A manuscript in revision is not an accepted or published article.</p></section><p class="record-note">${e(d.notes.bibliography)} DOI links are provided where verified; other records link to an explicitly labeled Scholar search.</p></section>`;
}

export function projects(d) {
  return `<section class="section shell research-projects" id="projects"><div class="section-heading"><div><h2>Projects &amp; collaborative programs</h2></div></div><nav class="section-nav" aria-label="Project index">${d.researchProjects.map((p,i)=>`<a href="#${p.id}">0${i+1} / ${e(p.title)}</a>`).join('')}<a href="#collaborations">Collaborative programs</a></nav>${d.researchProjects.map((p,i)=>`<article class="project-detail" id="${p.id}"><div class="project-heading">${label(`0${i+1} / ${p.status}`)}<h2>${e(p.title)}</h2><p class="research-question">${e(p.question)}</p></div><div class="project-body"><dl class="project-facts"><div><dt>My role</dt><dd>${e(p.role)}</dd></div><div><dt>Approach</dt><dd>${e(p.approach)}</dd></div><div><dt>${p.papers.length?'Key outcome':'Project status'}</dt><dd>${e(p.outcome)}</dd></div></dl>${tags(p.methods)}${p.papers.length?`<h3 class="small-heading">Related papers</h3><ul class="paper-references">${p.papers.map(id=>paperReference(d,id)).join('')}</ul>`:''}${p.patents.length?`<h3 class="small-heading">Related technology / patent records</h3><ul class="paper-references">${p.patents.map(i=>`<li><a href="patents.html#${d.patents[i].id}">${e(d.patents[i].englishTitle)}</a><span>${e(d.patents[i].jurisdiction)} · ${e(d.patents[i].number)}</span></li>`).join('')}</ul>`:''}</div></article>`).join('')}<section id="collaborations" class="collaborations">${heading('Research in context','Collaborative <em>programs.</em>')}<p class="section-intro">Research programs listed in my CV. Dates refer to the listed project periods. My individual responsibilities are identified only where the CV specifies them.</p><div class="program-list">${d.projects.map(p=>`<article class="program-row"><span class="period">${e(p.period)}</span><div><h3>${e(p.englishTitle)}</h3><p class="original-title" lang="ko">${e(p.title)}</p><p>${e(p.sponsor)}${p.personalRole?' · <strong>My role: '+e(p.personalRole)+'</strong>':''}</p></div></article>`).join('')}</div><p class="record-note">English project labels summarize the original Korean titles. Industry entries contain only the scope already supplied in the CV; no unpublished results or technical process details are included.</p></section></section>`;
}

export function patents(d) {
  return `${pageHero('Intellectual property','Patents &amp;<br><em>technology translation.</em>','Patent records connect materials and device research with semiconductor, memory, and optoelectronic technologies.')}<section class="section shell patent-section"><div class="section-intro"><p>Registration and application are different stages. Statuses, dates, inventors, and identifiers below follow the supplied CV.</p><p class="record-note">English labels summarize Korean titles; the original titles are retained. Related research links indicate a topic connection.</p></div><div class="patent-list">${d.patents.map((p,i)=>`<article class="patent-item" id="${p.id}"><div class="patent-number">${String(i+1).padStart(2,'0')}<span class="badge ${p.status==='Registered'?'':'badge-outline'}">${e(p.status)}</span></div><div><h2>${e(p.englishTitle)}</h2><p class="original-title" ${/[가-힣]/.test(p.title)?'lang="ko"':''}>${e(p.title)}</p><dl class="patent-facts"><div><dt>Inventors</dt><dd lang="ko">${e(p.inventors)}</dd></div><div><dt>Jurisdiction</dt><dd>${e(p.jurisdiction)}</dd></div><div><dt>${p.status==='Registered'?'Registration number':'Application number'}</dt><dd>${e(p.number)}</dd></div><div><dt>${p.status==='Registered'?'Registration date':'Filing date'}</dt><dd>${e(p.date)}</dd></div></dl>${p.territoryNote?`<p class="record-note">${e(p.territoryNote)}</p>`:''}${tags(p.tags)}${textLink('research.html#'+p.relatedResearch,'Related research')}</div></article>`).join('')}</div></section>`;
}

export function about(d) {
  return `${pageHero('About / Min Jong Lee','From molecular interfaces<br>to <em>memory devices.</em>','Research background, current work, and the questions guiding my next steps.')}<section class="section shell about-grid">${portrait(d)}<div class="biography"><h2>Min Jong Lee <span lang="ko">이민종</span></h2><p class="position">${e(d.profile.position)}<br>${e(d.profile.department)}, ${e(d.profile.institution)}</p><p class="lead">${e(d.profile.bio)}</p><p>${e(d.profile.vision)}</p>${profiles(d)}${environment(d)}</div></section><section class="section shell about-trajectory">${heading('Research identity','Interface &amp; defect engineering<br>for <em>memory and computing.</em>')}<p class="section-intro">My questions have evolved from charge-selective contacts in organic electronics toward defects, ionic dynamics, and emerging memory. The next step is to carry this device-level understanding toward integrated hardware.</p>${trajectory(d)}</section><section class="section shell background-grid"><div>${label('Education')}<h2>Academic <em>foundation.</em></h2></div><div class="education"><span class="period">${e(d.profile.education.period)}</span><h3>${e(d.profile.education.degree)}</h3><p>${e(d.profile.education.school)}<br>Advisor: ${e(d.profile.advisor)}</p><div class="background-entry"><h3>Research background</h3><p>First-author work in artificial synapses, optoelectronic memristors, molecular contacts, and ALD oxide electrodes. Current doctoral research addresses the precision limits of ionic multilevel memory.</p>${textLink('research.html#projects','Projects & collaborative programs')}</div></div></section>${activities(d)}`;
}

export function activities(d) {
  const list = category => d.awards.filter(p=>p.category===category).sort((a,b)=>b.year-a.year).map(a=>`<article class="activity-row"><span class="period">${a.year}</span><div><h3>${e(a.englishTitle)}</h3><p lang="ko">${e(a.title)}</p></div></article>`).join('');
  return `<div class="section shell about-recognition" id="recognition"><section class="activity-section">${heading('Recognition','Awards &amp; <em>honors.</em>')}<div>${list('award')}</div></section><section class="activity-section">${heading('Academic support','Scholarships &amp; <em>researcher programs.</em>')}<div>${list('fellowship')}</div></section>${d.gallery.some(g=>g.image)?`<section class="activity-section" id="gallery">${heading('Research gallery','People, places &amp; <em>practice.</em>')}<div class="gallery-grid">${d.gallery.filter(g=>g.image).map(g=>`<figure><div class="gallery-image"><img src="${e(g.image)}" alt="${e(g.alt)}" width="900" height="675" loading="lazy" decoding="async"></div><figcaption><h3>${e(g.title)}</h3><p>${e(g.caption)}</p></figcaption></figure>`).join('')}</div></section>`:''}<p class="record-note">Recognition follows the September 2026 CV. Original Korean award names are retained alongside descriptive English translations.</p></div>`;
}

export function cv(d) {
  const pubs=d.publications.filter(p=>!p.status).sort((a,b)=>b.year-a.year);
  return `<div class="shell cv-toolbar">${textLink(d.profile.cv,'Download CV (PDF)','download')}<button class="text-link print-button" type="button" hidden>Print CV <span aria-hidden="true">↗</span></button><span>Public version · Record updated ${e(d.profile.updated)}</span></div><article class="cv-document shell"><header><p class="eyebrow">Curriculum vitae</p><h1>Min Jong Lee</h1><p>School of Electrical Engineering, Korea University<br>Seoul, Republic of Korea · <a href="mailto:${e(d.profile.email)}">${e(d.profile.email)}</a><br><a href="https://minjonglee.github.io/">minjonglee.github.io</a> · <a href="${e(d.scholar)}">Google Scholar</a></p></header><section><h2>Research identity</h2><p>Interface &amp; Defect Engineering for Memory and Computing</p><p>Research interests: memristor engineering for neuromorphic and compute-in-memory functions; DRAM MIM capacitors; ALD-based thin films; electrical characterization; charge transport and trap / defect dynamics. Integrated memory and computing is a long-term direction.</p></section><section><h2>Education</h2><p><strong>${e(d.profile.education.degree)}</strong> · ${e(d.profile.education.period)}<br>${e(d.profile.education.school)}<br>Advisor: ${e(d.profile.advisor)}</p></section><section><h2>Publications</h2><ol class="cv-publications">${pubs.map(p=>`<li><p>${authors(p.authors)}. “${e(p.title)}.” <em>${e(p.journal)}</em> (${p.year}).${p.type==='first'?' <strong>[First author]</strong>':''}${p.doi?` <a href="https://doi.org/${e(p.doi)}">doi:${e(p.doi)}</a>`:''}</p></li>`).join('')}</ol></section><section><h2>Manuscript in revision</h2>${d.publications.filter(p=>p.status).map(p=>`<p>${authors(p.authors)}. “${e(p.title)}.” ${e(p.journal)} (${p.year}). <strong>${e(p.status)}; not published.</strong></p>`).join('')}</section><section><h2>Patents</h2><ol>${d.patents.map(p=>`<li><p><span lang="ko">${e(p.inventors)}</span>. “<span ${/[가-힣]/.test(p.title)?'lang="ko"':''}>${e(p.title)}</span>.” ${e(p.status)} · ${e(p.jurisdiction)} · ${e(p.number)} · ${e(p.date)}.${p.territoryNote?' '+e(p.territoryNote):''}</p></li>`).join('')}</ol></section><section><h2>Research projects</h2>${d.projects.map(p=>`<div class="cv-entry"><p><strong>${e(p.englishTitle)}</strong><br><span lang="ko">${e(p.title)}</span><br>${e(p.sponsor)} · ${e(p.period)}${p.personalRole?' · '+e(p.personalRole):''}</p></div>`).join('')}</section><section><h2>Awards &amp; academic programs</h2>${d.awards.map(a=>`<div class="cv-entry"><p><strong>${a.year} · ${e(a.englishTitle)}</strong><br><span lang="ko">${e(a.title)}</span></p></div>`).join('')}</section><p class="cv-source">Public CV adapted from the record updated ${e(d.profile.updated)}. English labels for Korean project and award titles are descriptive translations. † Equal contribution; * Corresponding author. Original phone number and detailed postal address omitted.</p></article>`;
}
